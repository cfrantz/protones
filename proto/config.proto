syntax = "proto3";
package proto;

import "proto/controller.proto";

message Configuration {
    string path = 1; // For internal use by the config loader
    repeated string load = 2;
    ControllerModel controls = 3;
};
